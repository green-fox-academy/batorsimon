#include <stdio.h>
#include <stdlib.h>

#include "g_code.h"
#include "read_from_file.h"

int main()
{

    read_complete_file();


    /*
    int buffer[couting_lines()];

    char *p;
    for(int i = 0; i < couting_lines(); i++){
        p = read_file_line_by_line();
        tokenizing(p);
    }
*/
    return 0;
}
