#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int f = 16, result;
	// please square root f's value

    //double sqrt(int f);
    //printf("%d\n", f);
    //printf("Square root of %d is %d\n", f, sqrt(f) );

    //double x = 4.0, result;

    result = sqrt(f);
    printf("The square root of %d is %d", f, result);
    return 0;
}
